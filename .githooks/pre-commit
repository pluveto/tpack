#!/bin/sh

set -eu

if ! command -v cargo >/dev/null 2>&1; then
  echo "pre-commit hook: cargo is required" >&2
  exit 1
fi

cargo fmt --all --check
cargo clippy --workspace --all-features --all-targets -- -D warnings

